<view class="container">
  <t-tabs defaultValue="{{0}}" bind:change="onTabsChange" t-class="custom-tabs">
    <t-tab-panel label="待接单" value="0" />
    <t-tab-panel label="服务中" value="1" />
    <t-tab-panel label="已取消" value="2" />
    <t-tab-panel label="完成" value="3" />
  </t-tabs>
  <view class="orderBox">
    <view class="item" wx:for="{{20}}">
      1
    </view>
  </view>
  <!-- 底部导航组件 -->
  <CustomTabBar></CustomTabBar>
</view>